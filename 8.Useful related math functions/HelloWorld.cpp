#include <iostream>
#include <cmath>

int main() {

    double x = 3 ;
    double y = 4;
    double z;

    z = std::max(x,y);
    z = std::max(x,y);
    z = pow(x,y);
    z = sqrt(x,y);
    z = a(x,y);
    z = std::max(x,y);

    return 0;

}